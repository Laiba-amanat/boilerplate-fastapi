# Python Path Configuration
PYTHONPATH=src

# Application Configuration
VERSION=1.0.0
APP_TITLE=Enterprise FastAPI Backend Template
PROJECT_NAME=YourProject
APP_DESCRIPTION=Enterprise FastAPI Backend Template - Ready to Use

# Security Configuration (Must be modified)
SECRET_KEY=your_secret_key_here_generate_with_openssl_rand_hex_32
SWAGGER_UI_USERNAME=admin
SWAGGER_UI_PASSWORD=your_swagger_password_here

# Application Configuration
APP_ENV=development
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=240

# Database Configuration
DB_ENGINE=sqlite
DB_HOST=localhost
DB_PORT=5432
DB_USER=postgres
DB_PASSWORD=your_database_password_here
DB_NAME=your_database_name

# CORS Configuration - Modify according to actual situation
CORS_ORIGINS=http://localhost:3000,http://localhost:8080,http://127.0.0.1:3000

# Debug Mode (Set to False in production environment)
DEBUG=True
